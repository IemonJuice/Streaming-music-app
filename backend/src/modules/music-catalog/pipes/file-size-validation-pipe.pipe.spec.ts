import { FileSizeValidationPipePipe } from './file-size-validation-pipe.pipe';

describe('FileSizeValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new FileSizeValidationPipePipe()).toBeDefined();
  });
});
